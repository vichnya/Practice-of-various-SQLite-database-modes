[tool.poetry]
name = "sqlite-memory-db"
version = "0.2.0"
description = "Учебный проект по работе с SQLite в Python"
authors = ["Victoria Chernysheva"]

[tool.poetry.dependencies]
python = ">=3.8.0,<3.9"
numpy = "^1.22.2"
pendulum = "^2.1.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
